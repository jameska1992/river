import { screen, heading } from './styles'

export default function SearchPage() {
  return (
    <div style={screen}>
      <h1 style={heading}>Search</h1>
      <p style={{ color: 'var(--text-muted)' }}>Coming soon.</p>
    </div>
  )
}
