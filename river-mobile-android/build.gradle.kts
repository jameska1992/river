// Top-level build file. Plugins are declared without `apply` so the
// versions resolve here and individual modules just opt-in by id.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "2.0.20" apply false
}
